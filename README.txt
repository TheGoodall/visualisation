REQUIREMENTS:
	VTK
	numpy
	opencv-python
	Pillow

to run, just run:
python vis.py

will generate gen.png which is complete galaxy image

galaxy image must also be in same directory and named "galaxy.png"

Slider at bottom controls scale multiplyer, and toggle button in bottom left controls whether constellations are shown
